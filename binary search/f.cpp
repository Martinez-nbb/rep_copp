#include <iostream>
#include <algorithm>
#include <vector>
#include <iomanip>
using namespace std;
int main() {
	int l, n;
	cin >> n >> l;
    vector<double> a(n, 0);
	for (int i = 0; i < n; ++i) cin >> a[i];
}